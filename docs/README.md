**esphome-client**

***

<SPAN ALIGN="CENTER" STYLE="text-align:center">
<DIV ALIGN="CENTER" STYLE="text-align:center">

[![esphome-client: ESPHome Client API](https://raw.githubusercontent.com/hjdhjd/esphome-client/main/esphome-logo.svg)](https://github.com/hjdhjd/esphome-client)

# ESPHome Client API

[![Downloads](https://img.shields.io/npm/dt/esphome-client?color=%2318BCF2&logo=icloud&logoColor=%2318BCF2&style=for-the-badge)](https://www.npmjs.com/package/esphome-client)
[![Version](https://img.shields.io/npm/v/esphome-client?color=%2318BCF2&label=ESPHome%20Client%20API&logo=esphome&logoColor=%2318BCF2&style=for-the-badge)](https://www.npmjs.com/package/esphome-client)

## A complete Node-native ESPHome API client implementation with full protocol support.
</DIV>
</SPAN>

`esphome-client` is a TypeScript-first Node 22+ library that speaks the [ESPHome](https://esphome.io) native API end-to-end. It is zero-dependency, ESM-only, and ships a complete Noise Protocol Framework implementation (`Noise_NNpsk0_25519_ChaChaPoly_SHA256`) using only Node's built-in `crypto` and `net` modules.

This page is the rendered API reference. For installation, usage examples, core concepts (entity ids, telemetry, sub-APIs, schema extensions), and the v2 release notes, see the [README](https://github.com/hjdhjd/esphome-client#readme) and the [changelog](https://github.com/hjdhjd/esphome-client/blob/main/docs/Changelog.md). The module list below is the per-symbol reference - browse by module name or search for a specific export.

Public API barrel for the ESPHome client. Every consumer-facing symbol is re-exported here; per-symbol JSDoc lives at the symbol's defining module so each contract
has a single canonical home.

## Remarks

Consumers should import from the package root (`import { openEspHomeClient, entityId, ConfigurationError } from "esphome-client"`); deep imports into the
compiled `dist/` paths are not part of the public contract. Test helpers ship under the conditional export `esphome-client/testing`; they are intentionally not
re-exported here so production bundles don't pull in the mock-transport surface.

The set of barreled modules covers the connection/lifecycle surface (`esphome-client`, `lifecycle`, `health`, `reconnect`, `capabilities`), the entity model
(`entity-id`, `schemas`, `sub-device`), the named wire-enum constants (`api-constants`) and the API feature-version table (`api-feature-versions`), the sub-APIs
(`bluetooth-proxy`, `camera`, `home-assistant`, `serial-proxy`, `user-services`, `voice-assistant`, `zwave-proxy`), the typed error hierarchy (`errors`), the
stream backpressure options, and the protocol-level primitives (`protocol`, `crypto-noise`, `types`).

## Classes

| Class | Description |
| ------ | ------ |
| [AuthenticationError](classes/AuthenticationError.md) | Authentication failed: device rejected the supplied password. Permanent because retrying the same wrong password will not succeed. |
| [BackpressureError](classes/BackpressureError.md) | Emitted into a stream operating in `backpressure: "throw"` mode when the high-water mark is exceeded. Carries the dropped-item count for diagnostics. |
| [BluetoothProxyApi](classes/BluetoothProxyApi.md) | Bluetooth-proxy sub-API. Single instance per client; created lazily on first access via [EspHomeClient.bluetooth](classes/EspHomeClient.md#bluetooth). |
| [BufferOverflowError](classes/BufferOverflowError.md) | The receive buffer accumulated more bytes than [EspHomeClientOpenOptions.maxRecvBufferBytes](interfaces/EspHomeClientOpenOptions.md#maxrecvbufferbytes) without producing a complete frame. The peer is sending garbage or has stalled mid-frame. |
| [CameraApi](classes/CameraApi.md) | Camera sub-API. Per-id instance. |
| [CameraStreamClosedError](classes/CameraStreamClosedError.md) | Operational failure: the bus stream backing [CameraApi.snapshot](classes/CameraApi.md#snapshot) closed before any image arrived for the requested camera id. |
| [CipherState](classes/CipherState.md) | CipherState manages the encryption state for a single direction of communication. Implements the CipherState object as specified in Noise Protocol Framework §5.1 using ChaCha20-Poly1305. |
| [ConfigurationError](classes/ConfigurationError.md) | Construction-time misconfiguration: bad PSK length, missing host, conflicting options, etc. Caught at the boundary so internal code can trust validated structures. |
| [ConnectionClosedByPeerError](classes/ConnectionClosedByPeerError.md) | Peer closed the connection cleanly or unexpectedly mid-session. |
| [ConnectionError](classes/ConnectionError.md) | Generic connection-lifecycle failure. Parent class for transport-level errors that aren't more specifically classified. |
| [ConnectionRefusedError](classes/ConnectionRefusedError.md) | Underlying TCP connection refused (device not listening yet, port closed). Transient: device may be booting. |
| [ConnectionTimeoutError](classes/ConnectionTimeoutError.md) | Connection attempt timed out before the device responded. Transient: typically the device is rebooting or briefly unreachable. |
| [DecodingError](classes/DecodingError.md) | Decoder failed to parse an inbound message. Usually indicates a protocol bug, malformed device firmware output, or a wire-format change we don't support yet. |
| [DecryptionFailedError](classes/DecryptionFailedError.md) | A single inbound noise frame failed the ChaCha20-Poly1305 tag check on an already-handshaked session. |
| [EncodingError](classes/EncodingError.md) | Encoder failed to serialize an outbound message. Indicates a bug or out-of-range field value. |
| [EncryptionKeyInvalidError](classes/EncryptionKeyInvalidError.md) | The supplied PSK is the wrong length, malformed, or rejected by the device. Consumer must provide the correct key. |
| [EncryptionKeyMissingError](classes/EncryptionKeyMissingError.md) | The device requires encryption but no PSK was provided in the client options. Consumer must configure the encryption key. |
| [EncryptionRequiredError](classes/EncryptionRequiredError.md) | The server sent the noise-protocol indicator while the client was in plaintext mode (no PSK configured). Consumer must supply a PSK. |
| [EspHomeClient](classes/EspHomeClient.md) | The ESPHome native-API client. The single high-level entry point for ESP8266/ESP32 devices running ESPHome firmware. |
| [EspHomeError](classes/EspHomeError.md) | Base class for every library-emitted error. |
| [FrameTooLargeError](classes/FrameTooLargeError.md) | Inbound frame exceeded [EspHomeClientOpenOptions.maxFrameBytes](interfaces/EspHomeClientOpenOptions.md#maxframebytes). Hard limit protects against malformed length-prefixes from a buggy or hostile device. |
| [HandshakeError](classes/HandshakeError.md) | Failure during the protocol handshake (plaintext or noise). |
| [HandshakeState](classes/HandshakeState.md) | HandshakeState manages the complete Noise protocol handshake, implementing the NNpsk0 pattern with optional prologue support. This class implements the HandshakeState object as specified in Noise Protocol Framework §5.3. After the handshake completes, the sendCipher and receiveCipher properties provide access to the encryption states for ongoing communication. |
| [HeartbeatStalledError](classes/HeartbeatStalledError.md) | Heartbeat liveness check exhausted its stall budget without inbound activity. The connection is presumed dead. |
| [HomeAssistantApi](classes/HomeAssistantApi.md) | Owns the entire Home-Assistant-bridge surface: the outbound subscribe-and-respond wire pipeline plus the memoized inbound-dispatcher context. Constructed once per [EspHomeClient](classes/EspHomeClient.md), which exposes this instance directly through its `homeAssistant` getter rather than through client-level delegate methods. |
| [IncompatibleApiVersionError](classes/IncompatibleApiVersionError.md) | Device announced an API major version outside the client's supported range. Permanent because no protocol exchange will resolve it. |
| [MalformedVarintError](classes/MalformedVarintError.md) | A varint exceeded the 10-byte stop-bit limit (the 64-bit varint maximum). Indicates malformed input. |
| [MessageTooManyFieldsError](classes/MessageTooManyFieldsError.md) | A protobuf message contained more fields than [EspHomeClientOpenOptions.maxFieldsPerMessage](interfaces/EspHomeClientOpenOptions.md#maxfieldspermessage). Bounds the decoder's allocation so a hostile or buggy device cannot exhaust memory. |
| [NegotiationFailedError](classes/NegotiationFailedError.md) | API version negotiation found no overlap between the client's supported range and the device's announced range. Permanent without consumer intervention. |
| [NoiseHandshakeError](classes/NoiseHandshakeError.md) | Failure during the Noise NNpsk0 handshake itself. Carries a narrowed [NoiseHandshakeErrorCode](type-aliases/NoiseHandshakeErrorCode.md) for precise dispatch. |
| [NoiseHandshakeTimeoutError](classes/NoiseHandshakeTimeoutError.md) | Noise handshake aborted because the per-step timeout elapsed. |
| [NotConnectedError](classes/NotConnectedError.md) | A command was issued before the client connected, or after it disconnected. Indicates a lifecycle ordering bug in consumer code. |
| [PeerClosedDuringNoiseError](classes/PeerClosedDuringNoiseError.md) | Peer (the device) closed the socket while the noise handshake was still in flight. Triggers fallback to plaintext when applicable. |
| [PermanentError](classes/PermanentError.md) | Marker class for errors that auto-reconnect should not retry. |
| [PlaintextHandshakeError](classes/PlaintextHandshakeError.md) | Failure during the plaintext handshake exchange (HelloRequest/HelloResponse, or ConnectRequest/ConnectResponse for password-authenticated devices). |
| [ProtocolError](classes/ProtocolError.md) | Generic wire-protocol error. Parent class for framing, encoding, and decoding failures. |
| [SerialProxyApi](classes/SerialProxyApi.md) | Serial-proxy sub-API. Single instance per client; created lazily on first access via [EspHomeClient.serial](classes/EspHomeClient.md#serial). |
| [TruncatedMessageError](classes/TruncatedMessageError.md) | A fixed-width or length-delimited field declared a width that runs past the end of the message body. Indicates a truncated or malformed inbound message - a field tag claims more bytes than the buffer holds. |
| [UnknownEntityTypeError](classes/UnknownEntityTypeError.md) | The client received a message referencing an entity type the schema registry doesn't know about. |
| [UnknownMessageTypeError](classes/UnknownMessageTypeError.md) | The client received a message type the dispatcher doesn't recognize. Often a forward-compat scenario: a newer device sent a message this client didn't expect. |
| [UnsupportedCapabilityError](classes/UnsupportedCapabilityError.md) | A command was issued for a capability the device does not expose (e.g., voice-assistant operation against a device without the voice-assistant feature flag). |
| [UserServicesApi](classes/UserServicesApi.md) | User-defined services sub-API. Exposes the discovered service catalog and the two execution paths (by key or by name). |
| [VoiceAssistantApi](classes/VoiceAssistantApi.md) | Voice-assistant sub-API. Single instance per client; created lazily on first access. |
| [ZWaveProxyApi](classes/ZWaveProxyApi.md) | Z-Wave-proxy sub-API. Single instance per client; created lazily on first access via [EspHomeClient.zwave](classes/EspHomeClient.md#zwave). |

## Interfaces

| Interface | Description |
| ------ | ------ |
| [BaseEntity](interfaces/BaseEntity.md) | Base entity interface containing fields common to all ESPHome entity types. Carries the same fields as the schema-derived shape under a conventional name for ergonomic consumer use. |
| [BitmaskField](interfaces/BitmaskField.md) | Defines a field that participates in a bitmask-aggregated has-pattern. Used by entity types where the wire format collapses every per-field "has" indicator into a single uint32 bitmask field instead of emitting one boolean per option (compare [HasPatternField](interfaces/HasPatternField.md)). The encoder ORs each present option's `bit` into a running mask, emits the value fields, then writes the aggregated mask under the schema's `bitmaskFieldNumber`. |
| [BluetoothGATTCharacteristic](interfaces/BluetoothGATTCharacteristic.md) | GATT characteristic metadata. The `properties` bitmask encodes the characteristic-properties bits (Read = 0x02, Write = 0x08, Notify = 0x10, etc.) as defined in the Bluetooth Core spec; this surface passes the value through unchanged for the consumer to bit-test. |
| [BluetoothGATTDescriptor](interfaces/BluetoothGATTDescriptor.md) | GATT descriptor metadata. Carried inside [BluetoothGATTCharacteristic.descriptors](interfaces/BluetoothGATTCharacteristic.md#descriptors); surfaced from [BluetoothProxyApi.getServices](classes/BluetoothProxyApi.md#getservices). The wire fields are (`uuid: repeated uint64`, `handle`, `short_uuid`) - 128-bit UUIDs arrive as two-element uint64 arrays; shorter assigned-number UUIDs arrive on `shortUuid`. |
| [BluetoothGATTService](interfaces/BluetoothGATTService.md) | GATT service metadata. Surfaced from [BluetoothProxyApi.getServices](classes/BluetoothProxyApi.md#getservices) as an array of services; the device streams services across multiple wire frames terminated by a sentinel, and the sub-API accumulates them transparently. |
| [BluetoothLERawAdvertisement](interfaces/BluetoothLERawAdvertisement.md) | One inbound BLE advertisement record. Fanned out from a batched `BluetoothLERawAdvertisementsResponse` so consumers see ads at single-ad granularity. The wire shape is the nested `BluetoothLERawAdvertisement` message defined in `api.proto` §`BluetoothLERawAdvertisementsResponse`. |
| [BluetoothScannerStateData](interfaces/BluetoothScannerStateData.md) | One inbound scanner-state push. The device emits the wire-level `BluetoothScannerStateResponse` frame whenever the scanner transitions through its state machine (IDLE -> STARTING -> RUNNING when activated, RUNNING -> STOPPING -> STOPPED when deactivated, FAILED on controller-level error). Pushes are unsolicited at the wire level - the client does not subscribe; the device emits whenever a transition happens. |
| [ClientCapabilities](interfaces/ClientCapabilities.md) | Structured capability record. Consumers gate on named capabilities rather than version numbers or raw bitfields. Version-gated capabilities derive from the `API_FEATURE_VERSIONS` table; subsystem-flag capabilities derive from `DeviceInfo` feature-flag bits. |
| [ClientEventsMap](interfaces/ClientEventsMap.md) | The complete set of events this client emits. Entity-keyed events are inherited from the module-private `SchemaEvents` map so the entry list tracks the schema registry as the single source of truth. Non-entity events are listed explicitly below. This interface enables strongly typed `.on()`, `.once()`, and `.stream()` overloads without resorting to `any`, and a typo in an entity event name surfaces as a compile error at the subscription site. |
| [ClientMetrics](interfaces/ClientMetrics.md) | Optional metrics interface for observability. Consumers wire this to their backend (StatsD, OpenTelemetry, Prometheus, Datadog, custom) to count frames, time operations, and gauge state. Tags are passed as a flat record so each backend can shape labels per its conventions. |
| [CommandPackedBitsField](interfaces/CommandPackedBitsField.md) | Command-side packed-bits field. Mirrors [InboundPackedBitsField](interfaces/InboundPackedBitsField.md) but allows each named bit to carry an optional `hasFieldBit` that the encoder ORs into the role's `bitmaskFieldNumber` (the has-bitmask carrier) when the consumer supplies the named boolean - signaling the firmware that the corresponding packed bit is meaningful regardless of whether the consumer set it true or false. |
| [CommandPackedBitSpec](interfaces/CommandPackedBitSpec.md) | One named bit within a [CommandPackedBitsField](interfaces/CommandPackedBitsField.md) (command role). Adds the optional `hasFieldBit` that the encoder ORs into the role's has-bitmask carrier when the consumer touches this named bit. Inbound `PackedBitSpec` deliberately omits this field so a misconfigured state/listEntities schema fails to compile rather than carrying a silently-ignored slot. |
| [CommandSchema](interfaces/CommandSchema.md) | Defines the command message structure for an entity type. |
| [ConnectionParams](interfaces/ConnectionParams.md) | Per-link connection parameters carried on `BluetoothSetConnectionParamsRequest` (id 145). |
| [ConnectionsFreeData](interfaces/ConnectionsFreeData.md) | Snapshot of the device's connection-slot capacity, pushed via `BluetoothConnectionsFreeResponse` (id 81). The device pushes this on subscribe and on every change so consumers can adapt to slot pressure dynamically (e.g., back off a probe loop when `free === 0`). |
| [ConnectionStateData](interfaces/ConnectionStateData.md) | Connection-state snapshot pushed by the device via `BluetoothDeviceConnectionResponse` (id 69). One message shape covers both the connect-completed and disconnect-completed transitions; the `connected` boolean is the discriminant. `mtu` carries the negotiated GATT MTU (only meaningful on `connected=true`); `error` carries a nonzero firmware-level error code when the transition failed (typical on `connected=false` for a failed connection attempt). |
| [ConstructionRetryConfig](interfaces/ConstructionRetryConfig.md) | Backoff configuration for the [openEspHomeClient](functions/openEspHomeClient.md) factory's bounded construction-retry loop. |
| [DeviceInfo](interfaces/DeviceInfo.md) | Device information received from the ESPHome device. This structure contains all metadata about the connected ESPHome device. |
| [DownConnectionHealth](interfaces/DownConnectionHealth.md) | Health of a record whose socket is down (disconnected or reconnecting). `connectedAtMs` is forbidden via `?: never` so a stale connect epoch can never leak onto a down record through an object spread - a transition that tried to would fail to compile. `encrypted` is narrowed to `false`: a down record is never on an encrypted wire. |
| [EntitySchema](interfaces/EntitySchema.md) | Complete schema definition for a single entity type. |
| [EspHomeClientOpenOptions](interfaces/EspHomeClientOpenOptions.md) | Options accepted by [openEspHomeClient](functions/openEspHomeClient.md). Inherits every [EspHomeClientOptions](interfaces/EspHomeClientOptions.md) field and adds the construction-retry knobs. |
| [EspHomeClientOptions](interfaces/EspHomeClientOptions.md) | Configuration options for creating an ESPHome client instance. These options control how the client connects to and communicates with ESPHome devices. |
| [ESPHomeHandshakeOptions](interfaces/ESPHomeHandshakeOptions.md) | Options for creating an ESPHome Noise handshake. This is a specialized version for connecting to ESPHome devices. |
| [EspHomeLogging](interfaces/EspHomeLogging.md) | Logging interface for the client. Defaults to console output. Consumers supplying their own implementation must define all four levels: `debug`, `error`, `info`, and `warn`. |
| [ExecuteServiceArgumentValue](interfaces/ExecuteServiceArgumentValue.md) | Represents an argument value when executing a service. |
| [FieldSpec](interfaces/FieldSpec.md) | Defines the wire format for a single protobuf field. |
| [HasPatternField](interfaces/HasPatternField.md) | Defines a field that uses the has_* pattern common in ESPHome commands. These fields have a boolean "has" field followed by the actual value field. |
| [HomeAssistantServiceEvent](interfaces/HomeAssistantServiceEvent.md) | Home Assistant service call event data. Emitted as the `homeassistantService` event when an ESPHome device triggers a `homeassistant.action` or `homeassistant.service` call expecting Home Assistant to execute the action. |
| [HomeAssistantStateRequest](interfaces/HomeAssistantStateRequest.md) | Home Assistant state request event data. Emitted as the `homeassistantStateRequest` event when an ESPHome device requests the state of a Home Assistant entity, typically when ESPHome has an `on_value` trigger that references Home Assistant state. |
| [InboundPackedBitsField](interfaces/InboundPackedBitsField.md) | Inbound (state / listEntities) packed-bits field. Defines a single proto uint32 wire field that packs multiple consumer-facing booleans into its bits. The decoder reads the packed field and surfaces each named bit as a boolean on the entity/state object. The interface intentionally constrains its `bits` record to [InboundPackedBitSpec](interfaces/InboundPackedBitSpec.md) so a misplaced `hasFieldBit` (command-only) on a state or listEntities schema is a compile error rather than dead code. |
| [InboundPackedBitSpec](interfaces/InboundPackedBitSpec.md) | One named bit within an [InboundPackedBitsField](interfaces/InboundPackedBitsField.md) (state or listEntities role). Carries the bit position only; the encoder's `hasFieldBit` contribution is not meaningful on inbound roles, so the type structurally excludes it. |
| [ListEntitiesSchema](interfaces/ListEntitiesSchema.md) | Defines the list entities response message structure for an entity type. |
| [LiveConnectionHealth](interfaces/LiveConnectionHealth.md) | Health of a record whose socket is up (connected or stalled). Carries `connectedAtMs` - the SSOT for "when this connection began" - from which live uptime is derived via [connectionUptimeMs](functions/connectionUptimeMs.md). Both `connected` and `stalled` are "socket up" states, so uptime stays live through a stall. |
| [LogEventData](interfaces/LogEventData.md) | Log event data emitted when log messages are received from the ESPHome device. These provide insight into the device's internal operation and debugging information. |
| [MessageEventData](interfaces/MessageEventData.md) | Message event data. This structure is emitted with the 'message' event for raw protocol messages. |
| [NoiseHandshakeOptions](interfaces/NoiseHandshakeOptions.md) | Options for creating a Noise handshake. |
| [NotifyDataChunk](interfaces/NotifyDataChunk.md) | One inbound GATT notify chunk. Emitted on the `bluetoothNotifyData` bus event whenever the device pushes a `BluetoothGATTNotifyDataResponse` (id 79); the [BluetoothProxyApi.notify](classes/BluetoothProxyApi.md#notify) iterator filters by `(address, handle)` so consumers see only the notifications they subscribed to. |
| [ReconnectConfig](interfaces/ReconnectConfig.md) | Auto-reconnect configuration. Every field has a sensible default; the empty object `{}` is the recommended way to opt in with defaults. |
| [RepeatedFieldSpec](interfaces/RepeatedFieldSpec.md) | Defines the wire format for a repeated protobuf field containing multiple values of the same type. |
| [RepeatedMessageFieldSpec](interfaces/RepeatedMessageFieldSpec.md) | Defines the wire format for a repeated protobuf field containing multiple sub-messages of the same shape. The wire bytes for each occurrence are decoded as their own protobuf message using [RepeatedMessageFieldSpec.fields](interfaces/RepeatedMessageFieldSpec.md#fields), and the resulting structured record is appended to the entity's surfaced array. Use this slot for fields like `MediaPlayerSupportedFormat` where the proto declares `repeated <NestedMessage>` and consumers need every nested scalar exposed without re-parsing raw bytes. |
| [SchemaExtensions](interfaces/SchemaExtensions.md) | Schema extension shape supplied to [extending](functions/extending.md). Allows the consumer to add scalar fields to listEntities or state without rewriting the upstream schema. |
| [SerialDataChunk](interfaces/SerialDataChunk.md) | One inbound serial-data chunk emitted on the `serialData` bus event. The `instance` field correlates with the [SerialProxyInfo](interfaces/SerialProxyInfo.md) index used at subscribe time so a single bus listener can route across multiple instances. |
| [SerialProxyAwaitOptions](interfaces/SerialProxyAwaitOptions.md) | Per-await options accepted by [SerialProxyApi.flush](classes/SerialProxyApi.md#flush) and [SerialProxyApi.getModemPins](classes/SerialProxyApi.md#getmodempins). The composed signal layers the caller's optional `AbortSignal` over the timeout via `AbortSignal.any`, mirroring the [VoiceAssistantApi.announce](classes/VoiceAssistantApi.md#announce) contract. |
| [SerialProxyConfigureOptions](interfaces/SerialProxyConfigureOptions.md) | Options accepted by [SerialProxyApi.configure](classes/SerialProxyApi.md#configure). Mirrors the wire-side `SerialProxyConfigureRequest` (field numbers per `api.proto`): `baudrate` (2), `flowControl` (3), `parity` (4), `stopBits` (5), `dataSize` (6). |
| [SerialProxyFlushResult](interfaces/SerialProxyFlushResult.md) | Result of [SerialProxyApi.flush](classes/SerialProxyApi.md#flush). The numeric `status` discriminates success (`OK` or `ASSUMED_SUCCESS`) from failure (`ERROR`, `TIMEOUT`, `NOT_SUPPORTED`); the optional `errorMessage` accompanies failure variants when the device supplies one. The `type` field always echoes `SerialProxyRequestType.FLUSH` for results surfaced by this method; it is included so the result shape parallels future request-types that may share the same response message. |
| [SerialProxyInfo](interfaces/SerialProxyInfo.md) | Per-instance metadata for a serial-proxy port advertised by the device. The array index in [DeviceInfo.serialProxies](interfaces/DeviceInfo.md#serialproxies) is the `instance` number used in every subsequent serial-proxy wire message. Empty (or undefined) when the device firmware was not compiled with `USE_SERIAL_PROXY`. |
| [ServiceArgument](interfaces/ServiceArgument.md) | One argument definition on a user-defined service. |
| [ServiceCallResult](interfaces/ServiceCallResult.md) | Result of a user-defined service execution. Emitted via the `serviceCallResult` event when the device reports back via `EXECUTE_SERVICE_RESPONSE` (only sent by firmwares that opt into `USE_API_USER_DEFINED_ACTION_RESPONSES`; older firmwares treat `executeService` as fire-and-forget and never produce this event). |
| [ServiceEntity](interfaces/ServiceEntity.md) | A user-defined service exposed by an ESPHome device. |
| [StateSchema](interfaces/StateSchema.md) | Defines the state response message structure for an entity type. |
| [StreamOptions](interfaces/StreamOptions.md) | Per-stream configuration for the async-iterable stream rails. |
| [SubDevice](interfaces/SubDevice.md) | One sub-device on a multi-device parent ESP. Returned in order from [EspHomeClient.subDevices](classes/EspHomeClient.md#subdevices). |
| [VoiceAssistantAnnounceOptions](interfaces/VoiceAssistantAnnounceOptions.md) | Options accepted by [VoiceAssistantApi.announce](classes/VoiceAssistantApi.md#announce). Mirrors the wire-side `VoiceAssistantAnnounceRequest`. |
| [VoiceAssistantAudioData](interfaces/VoiceAssistantAudioData.md) | Voice assistant audio data for streaming audio. |
| [VoiceAssistantAudioSettings](interfaces/VoiceAssistantAudioSettings.md) | Voice assistant audio settings for configuring audio processing. |
| [VoiceAssistantConfiguration](interfaces/VoiceAssistantConfiguration.md) | Voice assistant configuration response. |
| [VoiceAssistantEventData](interfaces/VoiceAssistantEventData.md) | Voice assistant event data that provides additional information about an event. |
| [VoiceAssistantRequest](interfaces/VoiceAssistantRequest.md) | Voice assistant request event data. |
| [VoiceAssistantTimerEventData](interfaces/VoiceAssistantTimerEventData.md) | Voice assistant timer event data. |
| [VoiceAssistantWakeWord](interfaces/VoiceAssistantWakeWord.md) | Voice assistant wake word configuration. |
| [WithReconnectClient](interfaces/WithReconnectClient.md) | Narrow seam [withReconnect](functions/withReconnect.md) drives. The host implements both `lifecycle()` (typed-iterable view of every connect/disconnect transition) and `health()` (the live snapshot - the helper reads it once at start to know whether a connect has already happened, in which case the body runs immediately rather than waiting for the next connect transition). |
| [WithReconnectOptions](interfaces/WithReconnectOptions.md) | Options accepted by [withReconnect](functions/withReconnect.md). |

## Type Aliases

| Type Alias | Description |
| ------ | ------ |
| [AlarmControlPanelCommand](type-aliases/AlarmControlPanelCommand.md) | Alarm control panel state commands accepted on the `command` field of `AlarmControlPanelCommandRequest` (see the `alarm_control_panel.command` schema). Mirrors `api.proto` `AlarmControlPanelCommand`. The request also carries an optional `code` string alongside this command, required when the entity's discovery-time `requiresCode` or `requiresCodeToArm` flag is set. |
| [AlarmControlPanelEntity](type-aliases/AlarmControlPanelEntity.md) | The `alarm_control_panel` entity type: arm / disarm / trigger transitions guarded by an optional code. |
| [AlarmControlPanelEvent](type-aliases/AlarmControlPanelEvent.md) | - |
| [AlarmControlPanelState](type-aliases/AlarmControlPanelState.md) | Alarm control panel state values reported by ESPHome alarm-control-panel entities on telemetry. Mirrors `api.proto` `AlarmControlPanelState`. Use this constant for narrowing on `AlarmControlPanelEvent.state` instead of raw numeric literals. |
| [BinarySensorEntity](type-aliases/BinarySensorEntity.md) | - |
| [BinarySensorEvent](type-aliases/BinarySensorEvent.md) | - |
| [BluetoothScannerMode](type-aliases/BluetoothScannerMode.md) | Bluetooth-proxy scanner-mode values accepted on `BluetoothScannerSetModeRequest` and reported on `BluetoothScannerStateResponse`. PASSIVE listens for advertisements without sending scan requests; ACTIVE additionally sends scan requests to elicit scan-response data from advertisers. Mirrors `api.proto` `BluetoothScannerMode` (§api.proto, `BluetoothScannerSetModeRequest.mode`). |
| [BluetoothScannerState](type-aliases/BluetoothScannerState.md) | Bluetooth-proxy scanner state values pushed by the device on `BluetoothScannerStateResponse`. The state machine transitions through IDLE -> STARTING -> RUNNING when scanning is activated, and RUNNING -> STOPPING -> STOPPED when deactivated. FAILED indicates the device's scanner refused to start (typically due to BT controller-level errors). Mirrors `api.proto` `BluetoothScannerState` (§api.proto, `BluetoothScannerStateResponse.state`). |
| [ButtonEntity](type-aliases/ButtonEntity.md) | The `button` entity type: a stateless momentary trigger (press-only, with no awaitable state). |
| [ButtonEvent](type-aliases/ButtonEvent.md) | - |
| [CameraEntity](type-aliases/CameraEntity.md) | - |
| [CameraEvent](type-aliases/CameraEvent.md) | - |
| [CameraStreamClosedErrorCode](type-aliases/CameraStreamClosedErrorCode.md) | Discriminated codes for [CameraStreamClosedError](classes/CameraStreamClosedError.md). |
| [ClimateAction](type-aliases/ClimateAction.md) | Climate actions that indicate the current activity of the HVAC system. These represent what the climate device is actively doing. Wire value 1 is intentionally absent: `api.proto` aligns action values with the matching `ClimateMode` values "for readability", and mode value 1 (`HEAT_COOL`) has no activity counterpart, so the sequence jumps from `OFF` (0) to `COOLING` (2). The upstream proto additionally defines `CLIMATE_ACTION_DEFROSTING = 7`, which this table does not yet enumerate; a device reporting that action therefore falls outside the named set surfaced through the climate schema's `action` enum mapping. |
| [ClimateEntity](type-aliases/ClimateEntity.md) | The `climate` entity type: HVAC mode, target setpoint(s), fan mode, preset, and swing. |
| [ClimateEvent](type-aliases/ClimateEvent.md) | - |
| [ClimateFanMode](type-aliases/ClimateFanMode.md) | Climate fan modes supported by ESPHome climate entities. These control how the fan operates within the HVAC system. |
| [ClimateFeature](type-aliases/ClimateFeature.md) | Climate capability bits packed into the `feature_flags` field on `ListEntitiesClimateResponse` (ESPHome API 1.14+). Mirrors the upstream firmware enum `ClimateFeatures` in `esphome/components/climate/climate_mode.h`. The schema's `packedBitsFields` declaration uses these bit values to surface each capability as a named boolean on the climate entity (`entity.supportsAction: boolean` etc.), with the deprecated per-capability boolean fields (proto fields 5, 6, 12, 22, 23) acting as fallbacks when `feature_flags` is absent on older firmware. |
| [ClimateMode](type-aliases/ClimateMode.md) | Climate modes supported by ESPHome climate entities. These define the primary operating state of HVAC systems. |
| [ClimatePreset](type-aliases/ClimatePreset.md) | Climate presets supported by ESPHome climate entities. These are predefined configurations for common scenarios. |
| [ClimateSwingMode](type-aliases/ClimateSwingMode.md) | Climate swing modes supported by ESPHome climate entities. These control the direction of airflow from the HVAC system. |
| [ColorMode](type-aliases/ColorMode.md) | Color modes supported by ESPHome light entities. These define the color control capabilities of lights. The numeric values are an upstream capability bitfield (`ColorMode` in `api.proto`): each entry ORs together the capability bits its mode requires, which is why the sequence is sparse (0, 1, 3, 7, 11, 19, 35, 39, 47, 51) rather than contiguous. The deprecated `COLOR_MODE_LEGACY_BRIGHTNESS = 2` is intentionally not exported - it was superseded by `BRIGHTNESS` (3). |
| [CommandFor](type-aliases/CommandFor.md) | Public command-options shape for a schema. Wire shape minus omitted fields plus added fields per the `CommandOverrides` entry. |
| [ConnectionHealth](type-aliases/ConnectionHealth.md) | Live connection-health snapshot. A discriminated union over [HealthState](variables/HealthState.md): [LiveConnectionHealth](interfaces/LiveConnectionHealth.md) carries the connect epoch (`connectedAtMs`) while the socket is up; [DownConnectionHealth](interfaces/DownConnectionHealth.md) forbids it while the socket is down. Uptime is never stored; it is derived from `connectedAtMs` via [connectionUptimeMs](functions/connectionUptimeMs.md). |
| [CoverEntity](type-aliases/CoverEntity.md) | The `cover` entity type: position, tilt, and open / close / stop operation with a current-operation state. |
| [CoverEvent](type-aliases/CoverEvent.md) | - |
| [CoverOperation](type-aliases/CoverOperation.md) | Cover operation states indicating what a cover is currently doing. |
| [DateEntity](type-aliases/DateEntity.md) | - |
| [DateEvent](type-aliases/DateEvent.md) | - |
| [DateTimeEntity](type-aliases/DateTimeEntity.md) | The `datetime` entity type: a combined date-and-time value. |
| [DateTimeEvent](type-aliases/DateTimeEvent.md) | - |
| [Entity](type-aliases/Entity.md) | Discriminated union of every entity in [ENTITY\_SCHEMAS](variables/ENTITY_SCHEMAS.md). Adding a new entity type to the schema registry extends this union automatically with no parallel declaration to maintain. |
| [EntityCategory](type-aliases/EntityCategory.md) | Entity-category classification reported on every `ListEntities*Response` payload (`entity_category` field). Mirrors `api.proto` `EntityCategory`. Use for filtering UI display lists ("show config entities separately from diagnostics") and for narrowing on `entity.entityCategory` rather than comparing against magic numbers. |
| [EntityFor](type-aliases/EntityFor.md) | Public entity shape for a schema. Combines the wire shape with any override entry for the entity type. Most entities have no override; the override layer is the exhaustive list of intentional wire-vs-API divergences. |
| [EntityId](type-aliases/EntityId.md) | Branded entity-id type. The phantom type parameter `T` carries the entity type at the type level only - no runtime cost. `EntityId<"light">` and `EntityId<"switch">` are distinct types that the type checker refuses to assign across, eliminating "passed wrong id to wrong API" bugs at compile time. |
| [EntityOf](type-aliases/EntityOf.md) | Convenience for narrowing an [Entity](type-aliases/Entity.md) to one entity type at the type level. |
| [EntityType](type-aliases/EntityType.md) | String literal union enumerating every ESPHome entity type. Used as the discriminant on the [Entity](type-aliases/Entity.md) union for narrowing. Derived directly from the keys of the [ENTITY\_SCHEMAS](variables/ENTITY_SCHEMAS.md) registry so the union and the schema cannot drift apart - adding a new entity type to ENTITY_SCHEMAS automatically extends this union. |
| [EnumMapping](type-aliases/EnumMapping.md) | Defines a mapping from user-friendly string values to protocol enum numbers. Used to transform string options into numeric values before encoding. |
| [EventEntity](type-aliases/EventEntity.md) | - |
| [EventEntityEvent](type-aliases/EventEntityEvent.md) | - |
| [ExtendedEntityType](type-aliases/ExtendedEntityType.md) | The typed entity-type union for a client parameterized by an [ExtraSchemaSet](type-aliases/ExtraSchemaSet.md). Resolves to the union of the built-in [EntityType](type-aliases/EntityType.md) keys and the extras keys, so an `EspHomeClient<{ door_cover: ... }>` accepts `"door_cover"` everywhere `EntityType` is accepted on the public surface. |
| [ExtraSchemaSet](type-aliases/ExtraSchemaSet.md) | A typed registry of additional schemas keyed by entity type. Each key becomes a valid `EntityType` for the client instance it's registered on; commands, telemetry, and entity discovery for that type pass through the same schema-driven machinery as built-in types. |
| [FanDirection](type-aliases/FanDirection.md) | Fan direction values reported by ESPHome fan entities and accepted on fan commands. Mirrors `api.proto` `FanDirection`. The command path also accepts the string keys (`"forward"` / `"reverse"`) per the schema's command `enumMappings`; consumers reading telemetry receive the numeric value and should narrow against this constant. |
| [FanEntity](type-aliases/FanEntity.md) | The `fan` entity type: on/off, speed level, oscillation, direction, and preset mode. |
| [FanEvent](type-aliases/FanEvent.md) | - |
| [HealthState](type-aliases/HealthState.md) | Connection-health states. Modeled as an `as const` object so the type narrows to a literal union without an enum. |
| [InfraredCapabilityFlags](type-aliases/InfraredCapabilityFlags.md) | Infrared entity capability bitmask flags. The wire-side `ListEntitiesInfraredResponse.capabilities` field is a bitwise OR of these values. Consumers bit-test against this constant to gate UI affordances (transmitter-only entities have no receiver pipeline; receiver-only entities cannot accept transmit commands). |
| [InfraredEntity](type-aliases/InfraredEntity.md) | The `infrared` entity type: transmits a raw mark/space timing pattern to a connected IR blaster. |
| [InfraredEvent](type-aliases/InfraredEvent.md) | The telemetry event for an `infrared` entity: a decoded inbound remote-control code received from the device. |
| [LifecycleEvent](type-aliases/LifecycleEvent.md) | Discriminated lifecycle event. Stalls and reconnect cycles surface on the [ConnectionHealth](type-aliases/ConnectionHealth.md) stream rather than here, by design - lifecycle is the boundary signal (we connected; we disconnected); health is the live observability surface. |
| [LightEntity](type-aliases/LightEntity.md) | The `light` entity type: on/off, brightness, color (RGB / white / color-temperature in mireds), and effects. |
| [LightEvent](type-aliases/LightEvent.md) | - |
| [LockCommand](type-aliases/LockCommand.md) | Lock commands accepted on the `command` field of `LockCommandRequest` (see the `lock.command` schema). Mirrors `api.proto` `LockCommand`. `OPEN` is only meaningful when the entity's discovery-time `supportsOpen` flag is set; the request also carries an optional `code` has-pattern field, required when `requiresCode` is set. |
| [LockEntity](type-aliases/LockEntity.md) | The `lock` entity type: lock / unlock / open with an optional code and a current lock state. |
| [LockEvent](type-aliases/LockEvent.md) | - |
| [LockState](type-aliases/LockState.md) | Lock state values reported by ESPHome lock entities on telemetry. Mirrors `api.proto` `LockState`. Use this constant for narrowing on `LockEvent.state` instead of raw numeric literals so call sites stay readable and survive future ESPHome wire-enum additions. |
| [LogLevel](type-aliases/LogLevel.md) | Log levels supported by ESPHome for log subscriptions. These control the verbosity of log messages received from the device. |
| [MediaPlayerCommand](type-aliases/MediaPlayerCommand.md) | Media player commands accepted on the has-pattern `command` field of `MediaPlayerCommandRequest` (see the `media_player.command` schema). Mirrors `api.proto` `MediaPlayerCommand`. Because `command`, `volume`, `mediaUrl`, and `announcement` are independent has-pattern fields on the same request, a single command call can combine, for example, a volume change with a play command in one round trip. |
| [MediaPlayerEntity](type-aliases/MediaPlayerEntity.md) | The `media_player` entity type: playback transport, volume, mute, and media-URL playback with announcements. |
| [MediaPlayerEvent](type-aliases/MediaPlayerEvent.md) | - |
| [MediaPlayerFormatPurpose](type-aliases/MediaPlayerFormatPurpose.md) | Media player supported-format purpose values reported on entity discovery. Mirrors `api.proto` `MediaPlayerFormatPurpose`. Use this constant for narrowing on `MediaPlayerEntity.supportedFormats[].purpose` instead of raw numeric literals. |
| [MediaPlayerState](type-aliases/MediaPlayerState.md) | Media player state values reported by ESPHome media player entities on telemetry. Mirrors `api.proto` `MediaPlayerState`. Use this constant for narrowing on `MediaPlayerEvent.state` instead of raw numeric literals. |
| [NoiseHandshakeErrorCode](type-aliases/NoiseHandshakeErrorCode.md) | Discriminated codes for noise-handshake failure modes that consumers can pattern-match on. |
| [NoiseRole](type-aliases/NoiseRole.md) | Role in the Noise protocol handshake. |
| [Nullable](type-aliases/Nullable.md) | Utility type that allows a value to be either the given type or `null`. |
| [NumberEntity](type-aliases/NumberEntity.md) | The `number` entity type: a bounded numeric value set within its min / max / step range. |
| [NumberEvent](type-aliases/NumberEvent.md) | - |
| [NumberMode](type-aliases/NumberMode.md) | Number-entity input mode, surfaced on `ListEntitiesNumberResponse` (`mode` field). Mirrors `api.proto` `NumberMode`. The mode tells the consumer how to render the number input - free-form auto, exact numeric box, or bounded slider. |
| [RadioFrequencyCapabilityFlags](type-aliases/RadioFrequencyCapabilityFlags.md) | Radio-frequency entity capability bitmask flags. The wire-side `ListEntitiesRadioFrequencyResponse.capabilities` field is a bitwise OR of these values. The two bits map identically to [InfraredCapabilityFlags](variables/InfraredCapabilityFlags.md): bit 0 = transmitter, bit 1 = receiver, per the comment at `api.proto` §ListEntitiesRadioFrequencyResponse.capabilities. |
| [RadioFrequencyEntity](type-aliases/RadioFrequencyEntity.md) | The `radio_frequency` entity type: transmits a raw 433.92 MHz OOK timing pattern to a connected RF module. |
| [RadioFrequencyEvent](type-aliases/RadioFrequencyEvent.md) | The telemetry event for a `radio_frequency` entity: a decoded inbound RF transmission received from the device. |
| [RadioFrequencyModulation](type-aliases/RadioFrequencyModulation.md) | Radio-frequency modulation values accepted on transmit requests and reported (as a bitmask) in `ListEntitiesRadioFrequencyResponse.supported_modulations`. The `supported_modulations` bitmask uses bit N to indicate that modulation value N is supported by the entity. Mirrors ESPHome's `esphome/components/radio_frequency/radio_frequency.h::RadioFrequencyModulation` enum. |
| [SchemaForExtended](type-aliases/SchemaForExtended.md) | Resolve an entity-type string to its [EntitySchema](interfaces/EntitySchema.md) type by consulting either the built-in [ENTITY\_SCHEMAS](variables/ENTITY_SCHEMAS.md) or the supplied [ExtraSchemaSet](type-aliases/ExtraSchemaSet.md). Drives the type-level threading on the public surface: `latest<T>()`, `command<T>()`, `commandAndAwait<T>()`, `snapshotFor<T>()`, `telemetryFor<T>()`, and `telemetryForId<T>()` all index through this helper so an extras-keyed entity type narrows to its declared schema's entity / event / command shape. |
| [SelectEntity](type-aliases/SelectEntity.md) | The `select` entity type: a single choice from a fixed set of options. |
| [SelectEvent](type-aliases/SelectEvent.md) | - |
| [SensorEntity](type-aliases/SensorEntity.md) | - |
| [SensorEvent](type-aliases/SensorEvent.md) | - |
| [SensorStateClass](type-aliases/SensorStateClass.md) | State-class classification for sensor entities, surfaced on `ListEntitiesSensorResponse` (`state_class` field). Mirrors `api.proto` `SensorStateClass`. The class tells the consumer how to interpret a numeric sensor reading over time (instantaneous measurement vs. monotonically increasing total vs. resetting total vs. angular measurement). |
| [SerialProxyLineStateFlags](type-aliases/SerialProxyLineStateFlags.md) | Modem-control line bitmask flags used by `SerialProxySetModemPinsRequest` and `SerialProxyGetModemPinsResponse`. RTS is bit 0, DTR is bit 1 - the standard UART modem-control set. Mirrors ESPHome's `SerialProxyLineStateFlags` enum (upstream header location not yet pinned down; the bit positions follow the standard UART convention used by every comparable driver). Consumers compose flags via bitwise OR (e.g., `SerialProxyLineStateFlags.RTS | SerialProxyLineStateFlags.DTR`) and read the response value as a bitmask. |
| [SerialProxyParity](type-aliases/SerialProxyParity.md) | Serial-proxy parity values accepted on `SerialProxyConfigureRequest`. Mirrors `api.proto` `SerialProxyParity` (§api.proto, `SerialProxyConfigureRequest.parity`). The three values are the only ones the upstream firmware accepts; ESPHome's serial-proxy component rejects any other value. |
| [SerialProxyPortType](type-aliases/SerialProxyPortType.md) | Serial-proxy port-type discriminant carried on each `SerialProxyInfo` entry advertised by the device in `DeviceInfoResponse.serial_proxies`. Mirrors `api.proto` `SerialProxyPortType` (§api.proto, `SerialProxyInfo.port_type`). The numeric value distinguishes the wiring topology so consumers can adapt timing or DTR/RTS use; the client is otherwise indifferent to the value. |
| [SerialProxyStatus](type-aliases/SerialProxyStatus.md) | Serial-proxy status code carried on `SerialProxyRequestResponse.status`. Mirrors `api.proto` `SerialProxyStatus` (§api.proto, `SerialProxyRequestResponse.status`). OK and ASSUMED_SUCCESS both indicate the requested operation completed (ASSUMED_SUCCESS is used when the device cannot verify completion but has no reason to suspect failure); ERROR, TIMEOUT, and NOT_SUPPORTED are the failure variants. |
| [ServiceArgType](type-aliases/ServiceArgType.md) | Service argument types supported by ESPHome user-defined services. Carved here so the discovery module can decode service entities without depending on the host. |
| [SirenEntity](type-aliases/SirenEntity.md) | The `siren` entity type: on/off with optional tone, duration, and volume. |
| [SirenEvent](type-aliases/SirenEvent.md) | - |
| [StateEventFor](type-aliases/StateEventFor.md) | Public state-event shape for a schema. Combines the wire shape with any override entry for the entity type. |
| [StreamBackpressureMode](type-aliases/StreamBackpressureMode.md) | Backpressure policy for stream consumers that fall behind. |
| [SwitchEntity](type-aliases/SwitchEntity.md) | The `switch` entity type: a simple boolean on/off control. |
| [SwitchEvent](type-aliases/SwitchEvent.md) | - |
| [TelemetryEvent](type-aliases/TelemetryEvent.md) | Discriminated union of every telemetry-event variant in [ENTITY\_SCHEMAS](variables/ENTITY_SCHEMAS.md). Same SSOT story as [Entity](type-aliases/Entity.md): derived directly from the schema registry, no parallel union to maintain. |
| [TelemetryEventOf](type-aliases/TelemetryEventOf.md) | Convenience for narrowing a [TelemetryEvent](type-aliases/TelemetryEvent.md) to one entity type at the type level. |
| [TelemetryEventType](type-aliases/TelemetryEventType.md) | Telemetry-event type discriminant union. Derived directly from [ENTITY\_SCHEMAS](variables/ENTITY_SCHEMAS.md) so the literal-string union extends automatically as new entity types are added. |
| [TemperatureUnit](type-aliases/TemperatureUnit.md) | Temperature units reported by climate and water-heater entities on `ListEntities*Response`. Mirrors `api.proto` `TemperatureUnit`. Carried by ESPHome firmware that advertises API minor 14 or higher with the temperature-unit extension; firmware that does not omits the field, and consumers should treat the unit as celsius by default in that case. Capability gate: `client.capabilities().climateTemperatureUnit`. |
| [TextEntity](type-aliases/TextEntity.md) | The `text` entity type: a free-form string value within the device's length and mode constraints. |
| [TextEvent](type-aliases/TextEvent.md) | - |
| [TextMode](type-aliases/TextMode.md) | Text-entity input mode, surfaced on `ListEntitiesTextResponse` (`mode` field). Mirrors `api.proto` `TextMode`. The mode tells the consumer whether to render the input as plaintext or as a password (masked) field. |
| [TextSensorEntity](type-aliases/TextSensorEntity.md) | - |
| [TextSensorEvent](type-aliases/TextSensorEvent.md) | - |
| [TimeEntity](type-aliases/TimeEntity.md) | - |
| [TimeEvent](type-aliases/TimeEvent.md) | - |
| [UpdateCommand](type-aliases/UpdateCommand.md) | Update commands accepted by ESPHome update entities. Mirrors `api.proto` `UpdateCommand`. The schema's command `enumMappings` also accepts the string keys (`"none"` / `"update"` / `"check"`); this constant gives consumers a named alternative that survives wire-enum additions. |
| [UpdateEntity](type-aliases/UpdateEntity.md) | - |
| [UpdateEvent](type-aliases/UpdateEvent.md) | - |
| [ValueType](type-aliases/ValueType.md) | Value types that describe how to interpret and encode/decode field data. |
| [ValveEntity](type-aliases/ValveEntity.md) | - |
| [ValveEvent](type-aliases/ValveEvent.md) | - |
| [ValveOperation](type-aliases/ValveOperation.md) | Valve operation states that indicate the current activity of a valve. These represent what the valve is actively doing. |
| [VoiceAssistantEvent](type-aliases/VoiceAssistantEvent.md) | Voice assistant pipeline-progress events reported by the client on `VoiceAssistantEventResponse.event_type` as an assistant run advances. Mirrors `api.proto` `VoiceAssistantEvent`. The values fall into paired start/end brackets for each pipeline stage (STT, intent, TTS, wake word, VAD) plus a standalone `ERROR` and the higher-numbered `TTS_STREAM_START` / `TTS_STREAM_END` / `INTENT_PROGRESS` additions used for streamed responses. |
| [VoiceAssistantRequestFlag](type-aliases/VoiceAssistantRequestFlag.md) | Voice assistant request flags carried as a bitmask on `VoiceAssistantRequest.flags`, the server-to-client message that starts an assistant run. Mirrors `api.proto` `VoiceAssistantRequestFlag`. `USE_VAD` tells the client to apply voice-activity detection to end the utterance automatically; `USE_WAKE_WORD` tells the client the run was triggered by a wake word rather than a manual start. |
| [VoiceAssistantSubscribeFlag](type-aliases/VoiceAssistantSubscribeFlag.md) | Voice assistant subscription flags that control what data is received. |
| [VoiceAssistantTimerEvent](type-aliases/VoiceAssistantTimerEvent.md) | Voice assistant timer lifecycle events reported by the client on `VoiceAssistantTimerEventResponse.event_type`, alongside the affected `timer_id`. Mirrors `api.proto` `VoiceAssistantTimerEvent`. Consumers use this to track assistant-set timers (e.g. "set a timer for five minutes") from creation through completion or cancellation. |
| [WaterHeaterEntity](type-aliases/WaterHeaterEntity.md) | - |
| [WaterHeaterEvent](type-aliases/WaterHeaterEvent.md) | - |
| [WaterHeaterMode](type-aliases/WaterHeaterMode.md) | Water heater operating modes per `api.proto` `WaterHeaterMode`. Selects the heating strategy on devices that expose multiple energy sources or efficiency profiles. |

## Variables

| Variable | Description |
| ------ | ------ |
| [AlarmControlPanelCommand](variables/AlarmControlPanelCommand.md) | Alarm control panel state commands accepted on the `command` field of `AlarmControlPanelCommandRequest` (see the `alarm_control_panel.command` schema). Mirrors `api.proto` `AlarmControlPanelCommand`. The request also carries an optional `code` string alongside this command, required when the entity's discovery-time `requiresCode` or `requiresCodeToArm` flag is set. |
| [AlarmControlPanelState](variables/AlarmControlPanelState.md) | Alarm control panel state values reported by ESPHome alarm-control-panel entities on telemetry. Mirrors `api.proto` `AlarmControlPanelState`. Use this constant for narrowing on `AlarmControlPanelEvent.state` instead of raw numeric literals. |
| [BluetoothScannerMode](variables/BluetoothScannerMode.md) | Bluetooth-proxy scanner-mode values accepted on `BluetoothScannerSetModeRequest` and reported on `BluetoothScannerStateResponse`. PASSIVE listens for advertisements without sending scan requests; ACTIVE additionally sends scan requests to elicit scan-response data from advertisers. Mirrors `api.proto` `BluetoothScannerMode` (§api.proto, `BluetoothScannerSetModeRequest.mode`). |
| [BluetoothScannerState](variables/BluetoothScannerState.md) | Bluetooth-proxy scanner state values pushed by the device on `BluetoothScannerStateResponse`. The state machine transitions through IDLE -> STARTING -> RUNNING when scanning is activated, and RUNNING -> STOPPING -> STOPPED when deactivated. FAILED indicates the device's scanner refused to start (typically due to BT controller-level errors). Mirrors `api.proto` `BluetoothScannerState` (§api.proto, `BluetoothScannerStateResponse.state`). |
| [ClimateAction](variables/ClimateAction.md) | Climate actions that indicate the current activity of the HVAC system. These represent what the climate device is actively doing. Wire value 1 is intentionally absent: `api.proto` aligns action values with the matching `ClimateMode` values "for readability", and mode value 1 (`HEAT_COOL`) has no activity counterpart, so the sequence jumps from `OFF` (0) to `COOLING` (2). The upstream proto additionally defines `CLIMATE_ACTION_DEFROSTING = 7`, which this table does not yet enumerate; a device reporting that action therefore falls outside the named set surfaced through the climate schema's `action` enum mapping. |
| [ClimateFanMode](variables/ClimateFanMode.md) | Climate fan modes supported by ESPHome climate entities. These control how the fan operates within the HVAC system. |
| [ClimateFeature](variables/ClimateFeature.md) | Climate capability bits packed into the `feature_flags` field on `ListEntitiesClimateResponse` (ESPHome API 1.14+). Mirrors the upstream firmware enum `ClimateFeatures` in `esphome/components/climate/climate_mode.h`. The schema's `packedBitsFields` declaration uses these bit values to surface each capability as a named boolean on the climate entity (`entity.supportsAction: boolean` etc.), with the deprecated per-capability boolean fields (proto fields 5, 6, 12, 22, 23) acting as fallbacks when `feature_flags` is absent on older firmware. |
| [ClimateMode](variables/ClimateMode.md) | Climate modes supported by ESPHome climate entities. These define the primary operating state of HVAC systems. |
| [ClimatePreset](variables/ClimatePreset.md) | Climate presets supported by ESPHome climate entities. These are predefined configurations for common scenarios. |
| [ClimateSwingMode](variables/ClimateSwingMode.md) | Climate swing modes supported by ESPHome climate entities. These control the direction of airflow from the HVAC system. |
| [ColorMode](variables/ColorMode.md) | Color modes supported by ESPHome light entities. These define the color control capabilities of lights. The numeric values are an upstream capability bitfield (`ColorMode` in `api.proto`): each entry ORs together the capability bits its mode requires, which is why the sequence is sparse (0, 1, 3, 7, 11, 19, 35, 39, 47, 51) rather than contiguous. The deprecated `COLOR_MODE_LEGACY_BRIGHTNESS = 2` is intentionally not exported - it was superseded by `BRIGHTNESS` (3). |
| [CoverOperation](variables/CoverOperation.md) | Cover operation states indicating what a cover is currently doing. |
| [ENTITY\_SCHEMAS](variables/ENTITY_SCHEMAS.md) | Schema definitions for every supported ESPHome entity type. Each schema provides the complete field mapping for encoding commands and decoding state responses. |
| [EntityCategory](variables/EntityCategory.md) | Entity-category classification reported on every `ListEntities*Response` payload (`entity_category` field). Mirrors `api.proto` `EntityCategory`. Use for filtering UI display lists ("show config entities separately from diagnostics") and for narrowing on `entity.entityCategory` rather than comparing against magic numbers. |
| [ESPHOME\_NOISE\_PROLOGUE](variables/ESPHOME_NOISE_PROLOGUE.md) | ESPHome Noise protocol prologue prefix used for all ESPHome API connections. This identifies the connection as using the ESPHome Native API protocol. |
| [FanDirection](variables/FanDirection.md) | Fan direction values reported by ESPHome fan entities and accepted on fan commands. Mirrors `api.proto` `FanDirection`. The command path also accepts the string keys (`"forward"` / `"reverse"`) per the schema's command `enumMappings`; consumers reading telemetry receive the numeric value and should narrow against this constant. |
| [HealthState](variables/HealthState.md) | Connection-health states. Modeled as an `as const` object so the type narrows to a literal union without an enum. |
| [InfraredCapabilityFlags](variables/InfraredCapabilityFlags.md) | Infrared entity capability bitmask flags. The wire-side `ListEntitiesInfraredResponse.capabilities` field is a bitwise OR of these values. Consumers bit-test against this constant to gate UI affordances (transmitter-only entities have no receiver pipeline; receiver-only entities cannot accept transmit commands). |
| [LockCommand](variables/LockCommand.md) | Lock commands accepted on the `command` field of `LockCommandRequest` (see the `lock.command` schema). Mirrors `api.proto` `LockCommand`. `OPEN` is only meaningful when the entity's discovery-time `supportsOpen` flag is set; the request also carries an optional `code` has-pattern field, required when `requiresCode` is set. |
| [LockState](variables/LockState.md) | Lock state values reported by ESPHome lock entities on telemetry. Mirrors `api.proto` `LockState`. Use this constant for narrowing on `LockEvent.state` instead of raw numeric literals so call sites stay readable and survive future ESPHome wire-enum additions. |
| [LogLevel](variables/LogLevel.md) | Log levels supported by ESPHome for log subscriptions. These control the verbosity of log messages received from the device. |
| [MediaPlayerCommand](variables/MediaPlayerCommand.md) | Media player commands accepted on the has-pattern `command` field of `MediaPlayerCommandRequest` (see the `media_player.command` schema). Mirrors `api.proto` `MediaPlayerCommand`. Because `command`, `volume`, `mediaUrl`, and `announcement` are independent has-pattern fields on the same request, a single command call can combine, for example, a volume change with a play command in one round trip. |
| [MediaPlayerFormatPurpose](variables/MediaPlayerFormatPurpose.md) | Media player supported-format purpose values reported on entity discovery. Mirrors `api.proto` `MediaPlayerFormatPurpose`. Use this constant for narrowing on `MediaPlayerEntity.supportedFormats[].purpose` instead of raw numeric literals. |
| [MediaPlayerState](variables/MediaPlayerState.md) | Media player state values reported by ESPHome media player entities on telemetry. Mirrors `api.proto` `MediaPlayerState`. Use this constant for narrowing on `MediaPlayerEvent.state` instead of raw numeric literals. |
| [NOISE\_DH\_LEN](variables/NOISE_DH_LEN.md) | Length of Diffie-Hellman public keys in bytes. |
| [NOISE\_MAX\_MESSAGE\_LEN](variables/NOISE_MAX_MESSAGE_LEN.md) | Maximum Noise protocol message length as specified in the Noise Protocol Framework. |
| [NOISE\_PSK\_LEN](variables/NOISE_PSK_LEN.md) | Required length for pre-shared keys in bytes. |
| [NumberMode](variables/NumberMode.md) | Number-entity input mode, surfaced on `ListEntitiesNumberResponse` (`mode` field). Mirrors `api.proto` `NumberMode`. The mode tells the consumer how to render the number input - free-form auto, exact numeric box, or bounded slider. |
| [RadioFrequencyCapabilityFlags](variables/RadioFrequencyCapabilityFlags.md) | Radio-frequency entity capability bitmask flags. The wire-side `ListEntitiesRadioFrequencyResponse.capabilities` field is a bitwise OR of these values. The two bits map identically to [InfraredCapabilityFlags](variables/InfraredCapabilityFlags.md): bit 0 = transmitter, bit 1 = receiver, per the comment at `api.proto` §ListEntitiesRadioFrequencyResponse.capabilities. |
| [RadioFrequencyModulation](variables/RadioFrequencyModulation.md) | Radio-frequency modulation values accepted on transmit requests and reported (as a bitmask) in `ListEntitiesRadioFrequencyResponse.supported_modulations`. The `supported_modulations` bitmask uses bit N to indicate that modulation value N is supported by the entity. Mirrors ESPHome's `esphome/components/radio_frequency/radio_frequency.h::RadioFrequencyModulation` enum. |
| [SensorStateClass](variables/SensorStateClass.md) | State-class classification for sensor entities, surfaced on `ListEntitiesSensorResponse` (`state_class` field). Mirrors `api.proto` `SensorStateClass`. The class tells the consumer how to interpret a numeric sensor reading over time (instantaneous measurement vs. monotonically increasing total vs. resetting total vs. angular measurement). |
| [SerialProxyLineStateFlags](variables/SerialProxyLineStateFlags.md) | Modem-control line bitmask flags used by `SerialProxySetModemPinsRequest` and `SerialProxyGetModemPinsResponse`. RTS is bit 0, DTR is bit 1 - the standard UART modem-control set. Mirrors ESPHome's `SerialProxyLineStateFlags` enum (upstream header location not yet pinned down; the bit positions follow the standard UART convention used by every comparable driver). Consumers compose flags via bitwise OR (e.g., `SerialProxyLineStateFlags.RTS | SerialProxyLineStateFlags.DTR`) and read the response value as a bitmask. |
| [SerialProxyParity](variables/SerialProxyParity.md) | Serial-proxy parity values accepted on `SerialProxyConfigureRequest`. Mirrors `api.proto` `SerialProxyParity` (§api.proto, `SerialProxyConfigureRequest.parity`). The three values are the only ones the upstream firmware accepts; ESPHome's serial-proxy component rejects any other value. |
| [SerialProxyPortType](variables/SerialProxyPortType.md) | Serial-proxy port-type discriminant carried on each `SerialProxyInfo` entry advertised by the device in `DeviceInfoResponse.serial_proxies`. Mirrors `api.proto` `SerialProxyPortType` (§api.proto, `SerialProxyInfo.port_type`). The numeric value distinguishes the wiring topology so consumers can adapt timing or DTR/RTS use; the client is otherwise indifferent to the value. |
| [SerialProxyStatus](variables/SerialProxyStatus.md) | Serial-proxy status code carried on `SerialProxyRequestResponse.status`. Mirrors `api.proto` `SerialProxyStatus` (§api.proto, `SerialProxyRequestResponse.status`). OK and ASSUMED_SUCCESS both indicate the requested operation completed (ASSUMED_SUCCESS is used when the device cannot verify completion but has no reason to suspect failure); ERROR, TIMEOUT, and NOT_SUPPORTED are the failure variants. |
| [ServiceArgType](variables/ServiceArgType.md) | Service argument types supported by ESPHome user-defined services. Carved here so the discovery module can decode service entities without depending on the host. |
| [TemperatureUnit](variables/TemperatureUnit.md) | Temperature units reported by climate and water-heater entities on `ListEntities*Response`. Mirrors `api.proto` `TemperatureUnit`. Carried by ESPHome firmware that advertises API minor 14 or higher with the temperature-unit extension; firmware that does not omits the field, and consumers should treat the unit as celsius by default in that case. Capability gate: `client.capabilities().climateTemperatureUnit`. |
| [TextMode](variables/TextMode.md) | Text-entity input mode, surfaced on `ListEntitiesTextResponse` (`mode` field). Mirrors `api.proto` `TextMode`. The mode tells the consumer whether to render the input as plaintext or as a password (masked) field. |
| [UpdateCommand](variables/UpdateCommand.md) | Update commands accepted by ESPHome update entities. Mirrors `api.proto` `UpdateCommand`. The schema's command `enumMappings` also accepts the string keys (`"none"` / `"update"` / `"check"`); this constant gives consumers a named alternative that survives wire-enum additions. |
| [ValveOperation](variables/ValveOperation.md) | Valve operation states that indicate the current activity of a valve. These represent what the valve is actively doing. |
| [VoiceAssistantEvent](variables/VoiceAssistantEvent.md) | Voice assistant pipeline-progress events reported by the client on `VoiceAssistantEventResponse.event_type` as an assistant run advances. Mirrors `api.proto` `VoiceAssistantEvent`. The values fall into paired start/end brackets for each pipeline stage (STT, intent, TTS, wake word, VAD) plus a standalone `ERROR` and the higher-numbered `TTS_STREAM_START` / `TTS_STREAM_END` / `INTENT_PROGRESS` additions used for streamed responses. |
| [VoiceAssistantRequestFlag](variables/VoiceAssistantRequestFlag.md) | Voice assistant request flags carried as a bitmask on `VoiceAssistantRequest.flags`, the server-to-client message that starts an assistant run. Mirrors `api.proto` `VoiceAssistantRequestFlag`. `USE_VAD` tells the client to apply voice-activity detection to end the utterance automatically; `USE_WAKE_WORD` tells the client the run was triggered by a wake word rather than a manual start. |
| [VoiceAssistantSubscribeFlag](variables/VoiceAssistantSubscribeFlag.md) | Voice assistant subscription flags that control what data is received. |
| [VoiceAssistantTimerEvent](variables/VoiceAssistantTimerEvent.md) | Voice assistant timer lifecycle events reported by the client on `VoiceAssistantTimerEventResponse.event_type`, alongside the affected `timer_id`. Mirrors `api.proto` `VoiceAssistantTimerEvent`. Consumers use this to track assistant-set timers (e.g. "set a timer for five minutes") from creation through completion or cancellation. |
| [WaterHeaterMode](variables/WaterHeaterMode.md) | Water heater operating modes per `api.proto` `WaterHeaterMode`. Selects the heating strategy on devices that expose multiple energy sources or efficiency profiles. |

## Functions

| Function | Description |
| ------ | ------ |
| [aliasOf](functions/aliasOf.md) | Builds a schema that aliases an existing entity type under a different name. The returned schema is a fresh shallow copy that reuses the upstream type's wire format verbatim and keeps the upstream `type` discriminant unchanged; the caller overrides the `type` field at registration time when the alias should surface under a new name (as the extras examples and routing tests do). |
| [connectionUptimeMs](functions/connectionUptimeMs.md) | Derive live connection uptime in milliseconds from a [ConnectionHealth](type-aliases/ConnectionHealth.md) record. Returns `now - connectedAtMs` while the socket is up (connected or stalled) and `0` while it is down - so uptime stays live through a stall and is structurally `0` when there is no connection. This is the single derivation of uptime from the `connectedAtMs` SSOT; callers pass `now` only in tests. |
| [createESPHomeHandshake](functions/createESPHomeHandshake.md) | Factory function to create a Noise handshake specifically for ESPHome connections. This function automatically configures the correct prologue for ESPHome Native API communication. ESPHome devices expect a specific prologue format and this function handles that setup automatically. |
| [createHandshake](functions/createHandshake.md) | Factory function to create a Noise handshake with a cleaner API. This is the primary way to create a handshake for general Noise protocol usage. For ESPHome specific connections, use [createESPHomeHandshake](functions/createESPHomeHandshake.md) instead. |
| [disconnectedCapabilities](functions/disconnectedCapabilities.md) | Returns the "disconnected" capability record. Used as the initial value in [EspHomeClient.capabilities](classes/EspHomeClient.md#capabilities) before the first successful connect, and as the result during a connect-failure window where `deviceInfo` is null. |
| [disconnectedHealth](functions/disconnectedHealth.md) | Returns the "disconnected" health record. Used as the initial value before the first successful connect, and after every clean teardown. |
| [entityId](functions/entityId.md) | Canonical constructor - the only sanctioned way to mint an EntityId. Encapsulates the `{type}-{object_id}` format rule and the lowercasing convention so every code path produces the same string for the same entity. |
| [extending](functions/extending.md) | Builds a schema that extends an upstream entity type with additional scalar fields. Returns a new schema with the upstream's listEntities and state field maps merged with the supplied additions. Field number collisions are not detected at compile time; the consumer is responsible for picking field numbers that don't conflict with the upstream schema. |
| [isConnectionLive](functions/isConnectionLive.md) | Type guard narrowing a [ConnectionHealth](type-aliases/ConnectionHealth.md) to the live variant (socket up: connected or stalled), which carries [LiveConnectionHealth.connectedAtMs](interfaces/LiveConnectionHealth.md#connectedatms). |
| [isEntityId](functions/isEntityId.md) | Runtime predicate for narrowing an untrusted string into a branded entity id of a specific type. The predicate matches when the string starts with `${type}-`; we do not validate that the entity actually exists at this point - that's a separate runtime check via [EspHomeClient.hasEntity](classes/EspHomeClient.md#hasentity) after the type narrowing. |
| [logLevelName](functions/logLevelName.md) | Resolve a numeric log level back to its canonical name. Used by diagnostic logging and consumer code that wants to display the level alongside the message. Falls back to a stable `Unknown(<id>)` placeholder when the level is outside the registered set so callers never see `undefined`. |
| [openEspHomeClient](functions/openEspHomeClient.md) | Factory function. Creates a new [EspHomeClient](classes/EspHomeClient.md), connects, and resolves the connected client. Permanent errors ([PermanentError](classes/PermanentError.md) subclasses) reject immediately; transient errors retry up to [EspHomeClientOpenOptions.maxConstructionRetries](interfaces/EspHomeClientOpenOptions.md#maxconstructionretries) times with backoff. |
| [parseEntityId](functions/parseEntityId.md) | Convenience for parsing an arbitrary string when the consumer doesn't yet know which entity type it points at. Returns the parsed `{ type, id }` pair when the prefix matches a known entity type, or `null` otherwise. |
| [withReconnect](functions/withReconnect.md) | Run a body callback once per successful connect. The helper subscribes to [WithReconnectClient.lifecycle](interfaces/WithReconnectClient.md#lifecycle), fires the body on the first observed `connect` event (or immediately if the client is already connected when the helper starts), and re-fires on every subsequent `connect` after a `disconnect`. The body's abort signal aborts the moment the matching `disconnect` arrives so re-entrant operations (`commandAndAwait`, `client.stream(...)` iterators) can wind down cleanly. |
